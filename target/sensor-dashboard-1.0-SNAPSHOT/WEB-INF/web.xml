<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Sensor Dashboard</display-name>

    <!-- CORS Filter -->
    <filter>
        <filter-name>CorsServletFilter</filter-name>
        <filter-class>com.backend.filter.CorsServletFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>CorsServletFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Jersey Servlet -->
    <servlet>
        <servlet-name>JerseyServlet</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>

        <!-- tell Jersey where your resource classes are -->
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>com.backend.resource</param-value>
        </init-param>   

        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>JerseyServlet</servlet-name>
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>

</web-app>
